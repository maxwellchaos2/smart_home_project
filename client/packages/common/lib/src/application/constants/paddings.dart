import 'package:flutter/material.dart';

abstract final class AppPaddings {
  static const horizontal = EdgeInsets.symmetric(horizontal: 20);
  static const scrollPadding = EdgeInsets.only(bottom: 30);
}
