name: client
description: "A new Flutter project."
publish_to: "none"
version: 0.9.9+23

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter

  dio: ^5.9.0

  common:
    path: packages/common

  assets:
    path: packages/assets

  themes:
    path: packages/themes

  app_info:
    path: packages/app_info

  auth_token:
    path: packages/auth_token

  auth:
    path: packages/auth

  localization:
    path: packages/localization

  devices:
    path: packages/devices

  events:
    path: packages/events

  rooms:
    path: packages/rooms

  floors:
    path: packages/floors

  inout_scenarios:
    path: packages/inout_scenarios

  device_scenarios:
    path: packages/device_scenarios

  yandex_auth:
    path: packages/yandex_auth

  users:
    path: packages/users

  zigbee_devices:
    path: packages/zigbee_devices

  device_positions:
    path: packages/device_positions

  projects:
    path: packages/projects

  wifi_devices:
    path: packages/wifi_devices

  flutter_svg: ^2.2.1

  hikvision_isapi:
    path: packages/hikvision_isapi

  media_kit: ^1.2.3

dependency_overrides:
  rxdart: ^0.27.7
  web: ^0.4.0
  js: ^0.7.0
  package_info_plus: ^6.0.0
  wakelock_plus: ^1.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

  integration_test:
    sdk: flutter

flutter:
  uses-material-design: true
